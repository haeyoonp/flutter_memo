<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:orientation="vertical"
    android:gravity="center_vertical"
    android:id="@+id/bottom_menu" >
    <item
        android:id="@+id/createFolder"
        android:layout_gravity="left"
        android:icon="@drawable/ic_create_folder"
        android:title="Create Folder"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/saveNote"
        android:icon="@drawable/ic_save_note"
        android:title="Save Note"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/openCamera"
        android:icon="@drawable/ic_open_camera"
        android:title="Open Camera"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/usePen"
        android:icon="@drawable/ic_use_pen"
        android:title="Use Pen"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/addNote"
        android:icon="@drawable/ic_add_note"
        android:title="Add Note"
        app:showAsAction="ifRoom" />

</menu>
